
export const theme = {
    colors: {
      primary: "#3498db",  // Ana renk (mavi)
      secondary: "#2ecc71",  // İkincil renk (yeşil)
      background: "#f4f4f4",  // Arka plan rengi
      text: "#333",  // Metin rengi
      modalBackground: "#fff", // Modal için arka plan rengi
      buttonBackground: "#2980b9", // Buton rengi
    },
    typography: {
      fontSize: "16px",  // Yazı tipi boyutu
      fontFamily: "'Arial', sans-serif",  // Yazı tipi ailesi
      fontWeight: "normal",  // Yazı tipi ağırlığı
    },
    spacing: {
      small: "8px",  // Küçük boşluk
      medium: "16px", // Orta büyüklükte boşluk
      large: "32px",  // Büyük boşluk
    },
    breakpoints: {
      mobile: "576px",  // Mobil cihazlar için kırılma noktası
      tablet: "768px",  // Tablet cihazlar için kırılma noktası
      desktop: "1024px",  // Masaüstü cihazlar için kırılma noktası
    },
  };
  